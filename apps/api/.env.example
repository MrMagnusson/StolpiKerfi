DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
PORT=4000
UPLOAD_DIR=./uploads
# Optional — enables POST /api/admin/seed (disabled/404 if unset). Only needed on a deployment where
# the database isn't directly reachable from a dev machine. See DEPLOY.md.
ADMIN_SEED_TOKEN=
